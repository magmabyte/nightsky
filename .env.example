DATABASE_URL="file:./dev.db"
JWT_SECRET="change-this-to-a-random-secret"

# On Railway, set DATABASE_URL to a path on a volume:
# DATABASE_URL="file:/data/toolshed.db"
# And set a real JWT_SECRET (any long random string)
